name: Test Self-Hosted Runner

on:
  workflow_dispatch:   # allows you to trigger manually
  push:                # also runs on every push

jobs:
  test-runner:
    runs-on: self-hosted   # 👈 ensures it uses your self-hosted runner
    steps:
      - name: Print message
        run: echo "✅ Hello from my self-hosted runner on $(hostname)"

      - name: Checkout repo
        uses: actions/checkout@v4

      - name: List files
        run: ls -lah
